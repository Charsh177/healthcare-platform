server:
  port: 8081
  servlet:
    context-path: /

spring:
  application:
    name: auth-service
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false
  datasource:
    url: jdbc:postgresql://localhost:5432/healthcare_db
    username: postgres
    password: healthcare_secure_password_123
    driver-class-name: org.postgresql.Driver
  security:
    user:
      name: admin
      password: admin123

jwt:
  secret: your-super-secret-key-min-32-characters-long-1234567890
  expiration: 900000
  refresh:
    expiration: 2592000000

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: INFO
    com.healthcare: DEBUG
    org.springframework.web: INFO

---
spring:
  config:
    activate:
      on-profile: docker
  datasource:
    url: jdbc:postgresql://postgres:5432/healthcare_db
    username: healthcare_user
    password: healthcare_secure_password_123
